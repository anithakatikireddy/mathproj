// initialize app
function start(app, express) {
console.log("ppp");
//	app.use(express.favicon(__dirname + '/public/images/favicon.ico'));		//set favicon
}

// release resources
function stop() {
	
}
